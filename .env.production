# ===========================================
# Al-Rehab Group Production Environment
# ===========================================
# IMPORTANT: Update these values before deployment!

# Database Configuration - PostgreSQL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
# Note: Special characters in password must be URL encoded (@ = %40, # = %23, etc.)
DATABASE_URL=postgresql://alrehabgroup_db_admin:Alrehab%40groupF0rExp0rt1@72.62.20.6:5432/alrehabgroup_db?schema=alrehab

# NextAuth Configuration
# NEXTAUTH_URL should be your production domain (e.g., https://alrehabgroup.com)
NEXTAUTH_URL=https://your-production-domain.com
# Generate a secure secret: openssl rand -base64 32
NEXTAUTH_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_IN_PRODUCTION

# App Configuration
NODE_ENV=production

# Optional: Analytics and tracking
# GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

# Optional: Email configuration for contact forms
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# Optional: File upload configuration
# MAX_FILE_SIZE=5242880
# UPLOAD_DIR=/var/www/uploads
